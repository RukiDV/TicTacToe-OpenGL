#include <iostream>
#include "Renderer.h"

int main(int argc, const char** args)
{
    constexpr int x = 1000;
    constexpr int y = 800;

    Renderer render_window(x, y);

    //TODO create event loop  
  
    return 0;
}